#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Doesn't align with rails, but prevents arbitrary CI breakage
# ARGV.unshift("--ensure-latest")

load Gem.bin_path("brakeman", "brakeman")
